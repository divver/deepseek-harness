[package]
name = "coop-board"
version = "0.2.1"
edition = "2021"
license = "MIT"
description = "Read-only layered coop dashboard (Ratatui) for the herdr coop plugin"

[[bin]]
name = "coop-board"
path = "src/main.rs"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
